# ThoughtTracker ML

Companion ML pipeline for ThoughtTracker.

This public repository is being assembled in reviewable layers from the finished
ThoughtTracker ML sandbox. Transcript corpus, inference modules, runtime
artifacts, owner scripts, tests, and documentation land in follow-up pull
requests so the ML story is easy to inspect.
